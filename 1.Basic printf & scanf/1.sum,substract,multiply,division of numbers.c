#include<stdio.h>
int main()
{
    int a,b,sum=0;
    printf("Enter a & b :");
    scanf("%d %d",&a,&b);

    sum=a+b;
    substract = a-b;
    multiply =a*b;
    division=a/b;

    printf("sum = %d",sum);
    printf("substract = %d",substract);
    printf("multiply = %d",multiply);
    printf("division = %d",division);
}
